using UnityEngine;

public class RoomSpawnPoint : MonoBehaviour
{

    //void OnTriggerEnter2D(Collider2D c)
    //{
    //    SingleRoom singleRoomScript = GameObject.FindGameObjectWithTag("Room").GetComponent<SingleRoom>();
    //    Rooms roomsScript = GameObject.FindGameObjectWithTag("Floor").GetComponent<Rooms>();

    //    if (c.gameObject.CompareTag("Room") && gameObject.CompareTag("RoomSpawnPoint"))
    //    {
    //        if (c.gameObject.transform.position == gameObject.transform.position)
    //        {
    //            if (transform.position.y == 42) singleRoomScript.openings[0].SetActive(false); Destroy(gameObject); //top
    //            if (transform.position.y == -42) singleRoomScript.openings[1].SetActive(false); Destroy(gameObject); //bottom
    //            if (transform.position.x == 54) singleRoomScript.openings[2].SetActive(false); Destroy(gameObject); //right
    //            if (transform.position.x == -54) singleRoomScript.openings[3].SetActive(false); Destroy(gameObject); //left
    //        }
    //    }
    //    else Destroy(gameObject);
    //}
        
    
}
